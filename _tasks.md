

type-hype.com
find-a-font.com
tip-tap-type.com
font-fetcher.com
font-fetch.com




use the below html as a starting point. i want to build it into a website for profit, but with a generous free tier for growth.
- hook it up to RAG to cache and reuse results when over a semantic threshold, then use AI as a fallback (saving the results)
- give 5-per-day and 15-per-week limit
- dont indicate to user that there is RAG/memory - they should believe it's always AI
- no subscription, just a credit-refill option with tiered pricing. 
- 'buy me a coffee' option also
- add common filters such as free/price, license type, etc independent of the AI/search
- the AI/search should result in both a user-facing response plus the font-list updating 






<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>AI Font Explorer</title> <script src="https://cdn.tailwindcss.com"></script> <link rel="preconnect" href="https://fonts.googleapis.com"> <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> <link id="dynamicFontLink" href="" rel="stylesheet"> <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet"> <style> body { font-family: 'Inter', sans-serif; transition: background-color 0.3s, color 0.3s; } .chat-scroll::-webkit-scrollbar { width: 6px; } .chat-scroll::-webkit-scrollbar-track { background: transparent; } .chat-scroll::-webkit-scrollbar-thumb { background-color: #cbd5e1; border-radius: 20px; } .dark .chat-scroll::-webkit-scrollbar-thumb { background-color: #4b5563; } /* Tooltip Logic: Triggered by Label Hover */ .tooltip-trigger:hover .tooltip-content { opacity: 1; transform: translateY(0); pointer-events: auto; } .tooltip-content { opacity: 0; transform: translateY(5px); transition: all 0.2s cubic-bezier(0.16, 1, 0.3, 1); pointer-events: none; } .loader { width: 16px; height: 16px; border: 2px solid #e2e8f0; border-bottom-color: #3b82f6; border-radius: 50%; display: inline-block; animation: rotation 1s linear infinite; } @keyframes rotation { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } } .skeleton { background: linear-gradient(90deg, #f1f5f9 25%, #e2e8f0 50%, #f1f5f9 75%); background-size: 200% 100%; animation: shimmer 1.5s infinite; } .dark .skeleton { background: linear-gradient(90deg, #1e293b 25%, #334155 50%, #1e293b 75%); } @keyframes shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } } </style> </head> <body class="bg-gray-50 text-gray-900 min-h-screen flex flex-col transition-colors duration-300 dark:bg-gray-950 dark:text-gray-100"> <div id="app" class="max-w-5xl mx-auto w-full p-4 md:p-8 flex-1 flex flex-col"> <header class="flex items-center justify-between mb-8"> <div class="flex items-center gap-3"> <div class="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center text-white font-bold text-lg">Aa</div> <h1 class="text-xl font-bold tracking-tight">Font Explorer <span class="text-blue-500 text-sm font-normal ml-1">AI</span></h1> </div> <button id="themeToggle" class="p-2 rounded-full hover:bg-gray-200 dark:hover:bg-gray-800 transition-colors text-gray-500 dark:text-gray-400"> <svg id="moonIcon" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 block dark:hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z" /></svg> <svg id="sunIcon" xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 hidden dark:block" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 9H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z" /></svg> </button> </header> <section class="mb-8 relative z-20"> <div id="chatContainer" class="bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 rounded-2xl shadow-sm transition-all duration-300 overflow-hidden"> <div id="chatHistory" class="hidden flex-col gap-3 p-4 max-h-60 overflow-y-auto chat-scroll border-b border-gray-100 dark:border-gray-800 bg-gray-50 dark:bg-gray-900/50"></div> <div class="p-4"> <div class="flex justify-between items-start mb-3"> <div id="agentStatus" class="text-sm text-gray-600 dark:text-gray-300 font-medium animate-pulse">Ready to explore typography.</div> <button id="expandChatBtn" class="text-xs text-blue-600 dark:text-blue-400 hover:underline flex items-center gap-1"> <span>Show History</span> <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path></svg> </button> </div> <div class="relative flex gap-2"> <input type="text" id="chatInput" placeholder="Describe the vibe..." class="flex-1 bg-gray-100 dark:bg-gray-800 text-gray-900 dark:text-white border-0 rounded-xl px-4 py-3 focus:ring-2 focus:ring-blue-500 focus:bg-white dark:focus:bg-gray-950 transition-all outline-none"> <button id="sendBtn" class="bg-blue-600 hover:bg-blue-700 text-white rounded-xl px-4 py-2 transition-colors flex items-center justify-center min-w-[50px]"> <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14M12 5l7 7-7 7"></path></svg> </button> </div> </div> </div> </section> <main class="flex-1 flex flex-col gap-8"> <div class="bg-white dark:bg-gray-900 p-4 rounded-xl border border-gray-200 dark:border-gray-800 flex flex-col md:flex-row gap-6 shadow-sm"> <div class="flex-1 flex flex-col gap-1.5"> <label class="text-[10px] font-bold uppercase tracking-wider text-gray-400">Preview Text</label> <div class="flex gap-2"> <input type="text" id="textInput" value="The quick brown fox jumps over the lazy dog." class="flex-1 bg-transparent border-b border-gray-200 dark:border-gray-700 py-1 text-base focus:border-blue-500 outline-none transition-colors dark:text-white"> <button id="pangramBtn" class="text-gray-400 hover:text-blue-500 transition-colors"><svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path></svg></button> </div> </div> <div class="w-full md:w-32 flex flex-col gap-1.5"> <label class="text-[10px] font-bold uppercase tracking-wider text-gray-400 flex justify-between">Size <span id="sizeVal">32</span></label> <input type="range" id="sizeInput" min="12" max="72" value="32" class="w-full h-1 bg-gray-200 dark:bg-gray-700 rounded-lg appearance-none cursor-pointer accent-blue-600"> </div> <div class="w-full md:w-32 flex flex-col gap-1.5"> <label class="text-[10px] font-bold uppercase tracking-wider text-gray-400 flex justify-between">Weight <span id="weightVal">400</span></label> <input type="range" id="weightInput" min="100" max="900" step="100" value="400" class="w-full h-1 bg-gray-200 dark:bg-gray-700 rounded-lg appearance-none cursor-pointer accent-blue-600"> </div> </div> <div id="fontGrid" class="grid grid-cols-1 md:grid-cols-2 gap-4"></div> <div id="pagination" class="hidden flex justify-center items-center gap-4 py-4 mt-auto"> <button id="prevPageBtn" class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 disabled:opacity-50 disabled:cursor-not-allowed transition-colors">Previous</button> <span id="pageIndicator" class="text-sm font-medium text-gray-500 dark:text-gray-400">Page 1 of 1</span> <button id="nextPageBtn" class="px-4 py-2 text-sm font-medium text-gray-600 dark:text-gray-300 bg-white dark:bg-gray-800 border border-gray-200 dark:border-gray-700 rounded-lg hover:bg-gray-50 dark:hover:bg-gray-700 disabled:opacity-50 disabled:cursor-not-allowed transition-colors">Next</button> </div> </main> </div> <script> const apiKey = ""; const state = { chatHistory: [], currentFonts: [], currentPage: 1, itemsPerPage: 8, isLoading: false, isChatExpanded: false }; const els = { grid: document.getElementById('fontGrid'), chatInput: document.getElementById('chatInput'), sendBtn: document.getElementById('sendBtn'), agentStatus: document.getElementById('agentStatus'), chatHistoryContainer: document.getElementById('chatHistory'), expandChatBtn: document.getElementById('expandChatBtn'), chatContainer: document.getElementById('chatContainer'), dynamicLink: document.getElementById('dynamicFontLink'), textInput: document.getElementById('textInput'), sizeInput: document.getElementById('sizeInput'), weightInput: document.getElementById('weightInput'), pangramBtn: document.getElementById('pangramBtn'), pagination: document.getElementById('pagination'), prevPageBtn: document.getElementById('prevPageBtn'), nextPageBtn: document.getElementById('nextPageBtn'), pageIndicator: document.getElementById('pageIndicator'), themeToggle: document.getElementById('themeToggle') }; async function queryGemini(userMessage) { state.isLoading = true; updateUIState(); const historyText = state.chatHistory.map(msg => `${msg.role === 'user' ? 'User' : 'Agent'}: ${msg.text}` ).join('\n'); const systemPrompt = `You are a typography expert. Strictly return JSON: { "reply": "string (max 15 words)", "fonts": [{ "name": "Name", "desc": "max 10 words", "category": "serif/sans-serif" }] }. Provide 24 fonts for pagination. Use real Google Fonts names.`; const fullPrompt = `History:\n${historyText}\n\nCurrent Request: ${userMessage}`; try { const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=${apiKey}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ contents: [{ parts: [{ text: fullPrompt }] }], systemInstruction: { parts: [{ text: systemPrompt }] }, generationConfig: { responseMimeType: "application/json" } }) }); const result = await response.json(); const jsonText = result.candidates?.[0]?.content?.parts?.[0]?.text; if (jsonText) { const data = JSON.parse(jsonText); state.chatHistory.push({ role: 'user', text: userMessage }); state.chatHistory.push({ role: 'model', text: data.reply }); if (data.fonts && data.fonts.length > 0) { state.currentFonts = data.fonts; state.currentPage = 1; loadGoogleFonts(data.fonts); } renderChatHistory(); els.agentStatus.innerHTML = `✨ ${data.reply}`; els.agentStatus.classList.remove('animate-pulse'); } } catch (e) { console.error(e); els.agentStatus.innerText = "Error connecting to AI."; } finally { state.isLoading = false; updateUIState(); renderGrid(); } } function loadGoogleFonts(fontList) { if (!fontList.length) return; const unique = [...new Set(fontList.map(f => f.name))]; const families = unique.map(name => `family=${name.trim().replace(/\s+/g, '+')}:wght@300;400;500;600;700`).join('&'); els.dynamicLink.href = `https://fonts.googleapis.com/css2?${families}&display=swap`; } function renderGrid() { els.grid.innerHTML = ''; if (state.isLoading) { renderSkeleton(); return; } if (state.currentFonts.length === 0) { els.grid.innerHTML = `<div class="col-span-full text-center text-gray-400 py-10 italic">Start a chat to find fonts...</div>`; els.pagination.classList.add('hidden'); return; } const start = (state.currentPage - 1) * state.itemsPerPage; const end = start + state.itemsPerPage; const pageFonts = state.currentFonts.slice(start, end); const totalPages = Math.ceil(state.currentFonts.length / state.itemsPerPage); pageFonts.forEach(font => { const card = document.createElement('div'); card.className = `font-card bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-800 rounded-xl p-4 transition-all duration-300`; card.innerHTML = ` <div class="flex justify-between items-center mb-3"> <div class="tooltip-trigger relative cursor-help flex flex-col group"> <span class="font-bold text-sm text-gray-900 dark:text-gray-100 border-b border-dotted border-gray-300 dark:border-gray-700 pb-0.5">${font.name}</span> <span class="text-[10px] uppercase tracking-wider text-gray-400 font-semibold">${font.category}</span> <!-- Tooltip anchored to the name --> <div class="tooltip-content absolute left-0 bottom-full mb-2 w-48 bg-gray-900 dark:bg-gray-100 text-white dark:text-gray-900 text-[11px] p-2 rounded-lg shadow-xl backdrop-blur-sm z-50"> <p class="font-medium">${font.desc}</p> <div class="absolute top-full left-4 border-8 border-transparent border-t-gray-900 dark:border-t-gray-100"></div> </div> </div> <a href="https://fonts.google.com/specimen/${font.name.replace(/\s+/g, '+')}" target="_blank" class="text-gray-300 hover:text-blue-500 transition-colors" title="View on Google Fonts"> <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path></svg> </a> </div> <div class="preview-text w-full overflow-hidden whitespace-nowrap text-ellipsis transition-all duration-200" style="font-family: '${font.name}', sans-serif; font-size: ${els.sizeInput.value}px; font-weight: ${els.weightInput.value};"> ${els.textInput.value} </div> `; els.grid.appendChild(card); }); els.pagination.classList.remove('hidden'); els.pageIndicator.textContent = `Page ${state.currentPage} of ${totalPages}`; els.prevPageBtn.disabled = state.currentPage === 1; els.nextPageBtn.disabled = state.currentPage === totalPages; } function renderSkeleton() { els.pagination.classList.add('hidden'); els.grid.innerHTML = Array(6).fill(0).map(() => ` <div class="bg-white dark:bg-gray-900 border border-gray-100 dark:border-gray-800 rounded-xl p-4"> <div class="flex justify-between mb-4"><div class="h-4 w-24 rounded skeleton"></div><div class="h-4 w-4 rounded skeleton"></div></div> <div class="h-8 w-3/4 rounded skeleton"></div> </div> `).join(''); } function renderChatHistory() { els.chatHistoryContainer.innerHTML = state.chatHistory.map((msg) => ` <div class="flex ${msg.role === 'user' ? 'justify-end' : 'justify-start'}"> <div class="max-w-[85%] text-sm px-3 py-2 rounded-xl ${msg.role === 'user' ? 'bg-blue-100 text-blue-900 dark:bg-blue-900/30 dark:text-blue-100' : 'bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-300'}">${msg.text}</div> </div>`).join(''); els.chatHistoryContainer.scrollTop = els.chatHistoryContainer.scrollHeight; } function updateUIState() { if (state.isLoading) { els.sendBtn.innerHTML = '<span class="loader border-white/30 border-b-white"></span>'; els.sendBtn.disabled = true; } else { els.sendBtn.innerHTML = '<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 12h14M12 5l7 7-7 7"></path></svg>'; els.sendBtn.disabled = false; } } function handleSend() { const val = els.chatInput.value.trim(); if (!val || state.isLoading) return; els.chatInput.value = ''; queryGemini(val); } els.sendBtn.addEventListener('click', handleSend); els.chatInput.addEventListener('keypress', (e) => e.key === 'Enter' && handleSend()); els.expandChatBtn.addEventListener('click', () => { state.isChatExpanded = !state.isChatExpanded; els.chatHistoryContainer.classList.toggle('hidden'); els.chatHistoryContainer.classList.toggle('flex'); els.expandChatBtn.innerHTML = state.isChatExpanded ? `<span>Hide History</span>` : `<span>Show History</span>`; }); [els.textInput, els.sizeInput, els.weightInput].forEach(el => { el.addEventListener('input', () => { document.getElementById('sizeVal').innerText = els.sizeInput.value; document.getElementById('weightVal').innerText = els.weightInput.value; const previews = document.querySelectorAll('.preview-text'); previews.forEach(p => { p.style.fontSize = `${els.sizeInput.value}px`; p.style.fontWeight = els.weightInput.value; if (el === els.textInput) p.textContent = els.textInput.value; }); }); }); els.pangramBtn.addEventListener('click', () => { const pangrams = ["The quick brown fox jumps over the lazy dog.", "Pack my box with five dozen liquor jugs.", "Sphinx of black quartz, judge my vow."]; els.textInput.value = pangrams[Math.floor(Math.random() * pangrams.length)]; els.textInput.dispatchEvent(new Event('input')); }); els.prevPageBtn.addEventListener('click', () => { if (state.currentPage > 1) { state.currentPage--; renderGrid(); } }); els.nextPageBtn.addEventListener('click', () => { if (state.currentPage < Math.ceil(state.currentFonts.length / state.itemsPerPage)) { state.currentPage++; renderGrid(); } }); els.themeToggle.addEventListener('click', () => document.body.classList.toggle('dark')); window.onload = () => queryGemini("Show me diverse, modern trending fonts for UI design."); </script> </body> </html>